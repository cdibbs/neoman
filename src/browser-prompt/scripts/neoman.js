$(document).ready(function() {
    // Should immediately establish WebSockets connection with neoman express server. This will allow us
    // to notify neoman of when the user has closed the browser, etc.

    // If no connection after X seconds browser-side, complain to user.
    // If no connection server-side, complain on the console.
});